# Contributing

Keep this example small and copy-pasteable.

```bash
npm ci
npm run typecheck
npm run build
npm run simulate
npm run audit:high
```

Do not commit bot tokens, wallet secrets, recovery codes, private keys, FROST material, nonces, signatures, or real user data.
